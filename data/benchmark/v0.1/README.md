# CrystalProbe v0.1 Curation Seed

This directory starts the v0.1 benchmark manifest in the canonical schema.

Important: every current record is `draft`. The CIF paths, source identifiers, and experimental evidence fields are placeholders for curation workflow testing. Do not use these records for scientific claims until each record is promoted to `reviewed` or `verified`.

The first verification pass should replace placeholders with CPOSS209 or open-literature CIF references, DOI-backed stability evidence, and explicit disorder annotations.

